
.row
  .large-12.columns
    %h3
      Merit Badges
      = link_to '[+]', new_manager_badge_path

    %ul
      - @badges.each do |badge|
        %li
          = badge.title
          - if badge.shaded_photo
            = image_tag badge.shaded_photo.photo.url(:small)
        
