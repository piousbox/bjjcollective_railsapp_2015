
overarching program

questpage = town (location)
  questset = badge = house (location)
    task = quest = exercise
    video
      task = quest = exercise

-=-----

a location can recursively contain other locations

